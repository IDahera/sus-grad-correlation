# susgrad runtime + dev requirements (Python 3.11+; developed on 3.14,
# the Docker images and CI use 3.12).
#
# Install torch/torchvision FIRST with the build that matches your machine --
# see https://pytorch.org/get-started/locally/ -- or use the Docker images,
# which pick the CPU or CUDA wheel index for you.

# Deep learning
torch>=2.2
torchvision>=0.17

# Classical ML (suspiciousness-prediction classifiers + tabular preprocessing)
scikit-learn>=1.3
numpy>=1.24

# CLI
click>=8.1

# Progress bars
tqdm>=4.65

# Visualisation (HTML heatmaps + comparison plots)
matplotlib>=3.7

# Testing
pytest>=8.0
